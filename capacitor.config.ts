import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ioniccodingexam',
  appName: 'ioniccodingexam',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
